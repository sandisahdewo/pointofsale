I want to initialize or scaffold the backend project, in the backend directory, now the directory is not exists, create the new directory
Install latest Golang version, use Chi router
Install latest PostgreSQL
Install latest Mailpit
Install latest Redis
Run the backend, frontend, database, redis and mailing services in the docker and docker compose
Use ORM for database interaction, migrations and seeding
Write secure SQL queries, use prepared statements and parameterized queries to prevent SQL injection attacks, write high performance queries
Use Argon2id for password hashing, JWT for authentication.
